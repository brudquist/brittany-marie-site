#project
  %h3= @project.name
  %p= @project.description

  -@project.module_images.each do |module_image|
    %p{ class: (request.xhr? ? 'xhr-request' : 'non-xhr-request') }
      =image_tag module_image.image_url, class: [:thumbnail, :'img-responsive']
